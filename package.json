{
  "name": "legacy-modernizer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "powershell -ExecutionPolicy Bypass -File ./start-dev.ps1",
    "dev:backend": "cd backend && python -m uvicorn main:app --host 127.0.0.1 --port 8010 --reload",
    "dev:frontend": "npm run dev --prefix frontend -- --host 127.0.0.1",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
